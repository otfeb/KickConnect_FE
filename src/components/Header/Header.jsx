import React from "react";
import styles from "./Header.module.css";

const Header = () => {
    return <div className={`${styles.header} ${styles.galindo}`}>Kickconnect</div>;
};

export default Header;